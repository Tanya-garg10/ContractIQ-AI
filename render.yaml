services:
  - type: web
    name: contractiq-ai
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    envVars:
      - key: PORT
        value: 10000
      - key: GEMINI_API_KEY
        sync: false
